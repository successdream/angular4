import { ExeUnlessDirective } from './exe-unless.directive';

describe('ExeUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new ExeUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
